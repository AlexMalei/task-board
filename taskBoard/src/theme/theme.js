const lightGreen = '#CEF9C6'
const white = '#FFFFFF'
const black = '#131313'
const darkGrey = '#EAEAEA'
const lightGrey = '#ECECEC'
const pampas = '#EEEBE5'

const error = '#CF1A17'

const colors = {
  black,
  white,

  lightGreen,

  lightGrey,
  darkGrey,

  pampas,

  error,
}

const sizes = [20, 60, 100]

const avatarSizes = {
  small: sizes[0],
  medium: sizes[1],
  large: sizes[2],
}

export const theme = {
  colors,
  avatarSizes,
}
