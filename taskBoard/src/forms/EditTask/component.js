import styled from 'styled-components'

import {
  StyledHeaderContainer as HeaderContainer,
  StyledHeaderText as HeaderText,
  MenuButton as StyledMenuButton,
} from '@/forms/AddTask/component'

export const StyledHeaderContainer = styled(HeaderContainer)``

export const StyledHeaderText = styled(HeaderText)``

export const MenuButton = styled(StyledMenuButton)``
