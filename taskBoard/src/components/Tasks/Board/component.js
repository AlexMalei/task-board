import styled from 'styled-components'

import {
  StyledContainer as StyledBoardContainer,
  StyledTitle as StyledBoardTitle,
  StyledBoardHeader as StyledHeader,
  StyledTasksContainer as StyledBoardTasksContainer,
} from '@/components/Board/component'

export const StyledContainer = styled(StyledBoardContainer)`
  margin: 0;
`

export const StyledTitle = styled(StyledBoardTitle)``

export const StyledBoardHeader = styled(StyledHeader)``

export const StyledTasksContainer = styled(StyledBoardTasksContainer)``
